# Copy to .env and set your Govee H6047 IP address
# Find it in the Govee app: Device Settings > LAN Control > Device IP
# Or leave unset to use UDP auto-discovery on startup
GOVEE_DEVICE_IP=192.168.x.x
